# OmniGuard — ملف مرجعي للمحادثة الجديدة

> هذا الملف ملخّص كامل لكل القرارات والاتفاقيات اللي وصلنالها لين الحين، عشان
> تفتح فيه محادثة جديدة بدون ما نفقد أي سياق. الصقه كامل بأول رسالة بالمحادثة
> الجديدة.

---

## 1. الصورة الكبيرة (المعمارية)

عندنا **عمليتين منفصلتين تماماً**، وكلاهما يتكلمون مع نفس قاعدة بيانات Supabase
عبر طبقة API واحدة بالنص:

```
┌─────────────────────┐         ┌──────────────────────┐
│  Discord Bot Process │         │  Dashboard (Browser)  │
│  (شغال 24/7)         │         │  (يفتحها المشرف بس)   │
│  index.js            │         │  general-settings.html│
│  commandhandler.js    │         │  dashboard.js         │
│  GRS.js               │         │  ...                   │
│  getroles.js          │         └──────────┬────────────┘
└──────────┬───────────┘                    │
           │                                 │
           ▼                                 ▼
┌─────────────────────────────────────────────────────┐
│         apiRouter.js  →  dbUtils.js / databaseQueries.js  →  db.js         │
│                                                       │
│                    Supabase (omniguard_schema_v5.sql) │
└─────────────────────────────────────────────────────┘
```

**القاعدة الذهبية:** أي شي لازم يصير *فوراً* بديسكورد نفسه (طرد، سكوت، تغيير
نيكنيم) → جهة البوت. أي شي المشرف يظبطه من متصفحه → جهة الداشبورد. الاثنين
يلتقون بس عند قاعدة البيانات.

---

## 2. جدول الملفات (وصلني / لسه ناقص)

### جهة البوت
| الملف | وظيفته | الحالة |
|---|---|---|
| `index.js` | نقطة البداية، يجمع كل شي، `client.login()` | ✅ وصلني |
| `client.js` | ينشئ اتصال ديسكورد الأساسي | ❌ ناقص |
| `commandhandler.js` | يقرأ أوامر الشات ويوجهها | ✅ وصلني (v3.2) |
| `GRS.js` | نيكنيم + فحص قنوات/رتب معفاة | ✅ وصلني |
| `getroles.js` | يزامن رتب/قنوات السيرفر لـ Supabase | ✅ وصلني (v1.1) |
| `ban.js`/`mute.js`/`warn.js`/`kick.js`/`delete.js` | تنفيذ أوامر الإشراف | ❌ ناقص (مهم لتأكيد `aliases`) |
| `AutoMod.js`, `AntiAlt.js` | فلترة رسائل، كشف حسابات وهمية | ❌ ناقص |
| `Rolemanagement.js`, `welcome.js` | رتب تلقائية، رسائل ترحيب | ❌ ناقص |
| `events/server.js` | أحداث انضمام/مغادرة الأعضاء | ❌ ناقص |
| `slashCommandsHandler.js` | أوامر السلاش | ❌ ناقص |
| `custom.js` | Embed Builder | ❌ ناقص |

### الجسر (يشتغل على Render)
| الملف | وظيفته | الحالة |
|---|---|---|
| `apiRouter.js` | Express routes بين الداشبورد وقاعدة البيانات | ✅ وصلني (v3.2) |
| `db.js` | الاتصال الخام بـ Supabase | ✅ وصلني (v3.0) |
| `dbUtils.js` | طبقة ترجمة يستخدمها البوت | ✅ وصلني (v3.2) |
| `databaseQueries.js` | الاستعلامات الفعلية | ✅ وصلني |
| `omniguard_schema_v5.sql` | تعريف كل الجداول والـ RLS | ✅ وصلني (v5.0 + v5.1 + v5.2) |

### جهة الداشبورد
| الملف | الحالة |
|---|---|
| `dashboard.js` | ✅ **جاهز نهائياً (v5.0)** — راجع القسم 4 |
| `general-settings.html` | ✅ **جاهزة نهائياً** — أول صفحة اتصلحت بالكامل |
| باقي صفحات HTML (`auto-mod.html`, `welcome-leave.html`, `role-management.html`, `anti-alt.html`, `leveling-system.html`, `custom-messages.html`, `logging.html`) | ❌ لسه ما وصلتني ولا وحدة منهم |
| صفحة/نظام تسجيل الدخول (OAuth2) | ❌ لسه ما بدأنا فيها (مؤجّلة عمداً للآخر) |

---

## 3. تنبيه مهم متكرر (لا تنساه بأي محادثة جديدة)

⚠️ **ما فيه مجلد اسمه `AutoMod` بالمشروع إطلاقاً.** كل ملفات البوت (بما فيها
`commandhandler.js`, `GRS.js`, `getroles.js`, `AutoMod.js`, `AntiAlt.js`,
`custom.js`...) موجودة **مباشرة** جوه `src/commands/` بدون أي تصنيف بمجلدات
فرعية. أي تعليق قديم بالكود يذكر مسار فيه `AutoMod/` كمجلد هو تعليق قديم غلط
من نسخة سابقة، ولازم يُتجاهل.

---

## 4. خطة Smart Binding (العقد اللي أي صفحة HTML لازم تلتزم فيه)

هذا هو "الدستور" اللي يخلي `dashboard.js` يشتغل تلقائياً على أي صفحة بدون
كود إضافي:

1. **`id` أي حقل = اسم عمود SQL بالضبط.** مثال: `<input id="prefix_bot">`
   لعمود `setting_guild.prefix_bot`.

2. **صندوق رتب أو قنوات من ديسكورد** (متعدد الاختيار، يتحول لتاقات):
   ```html
   <div class="selector-box"
        id="اسم_العمود"
        data-source="roles"        <!-- أو "channels" -->
        data-dropdown="dropdownId"
        onclick="toggleDropdown('dropdownId')">
       <span class="selector-placeholder">+ Select...</span>
   </div>
   <div id="dropdownId" class="dropdown-list"></div>
   ```
   - `data-source="roles"` → يتعبّى من `guild_structure_cache.roles` (مرتبة
     حسب التسلسل الهرمي، الأعلى أولاً).
   - `data-source="channels"` → يتعبّى من `guild_structure_cache.channels`
     (مجمّعة حسب الأقسام، نصية بس — Type 0/5).
   - كل تاق يخزّن الـ **Discord ID الحقيقي** بـ `data-id`، والاسم الظاهر بس
     للعرض. القيمة اللي تترسل لقاعدة البيانات هي الـ ID دايماً.

3. **تاقات نصية حرة** (زي كلمات ممنوعة، بدون قائمة جاهزة): نفس الشكل بس
   `class="filter-tags-box"` بدل `selector-box`، وفيها `<input class="tag-input-ghost">`
   داخلي يكتب فيه المستخدم ويضغط Enter.

4. **حقل من جدول مختلف عن جدول الصفحة الافتراضي** (نادر، بس صار فعلاً مرة
   وحدة لحد الحين): ضيف `data-target-table="اسم_الجدول_الصحيح"` على العنصر.
   مثال حي: `dashboard_access_roles` بصفحة `general-settings.html` — الصفحة
   كلها مربوطة بـ `setting_guild`، لكن هالعمود بجدول `setting_management_role`،
   فحطينا `data-target-table="setting_management_role"` عليه وخلاص —
   `dashboard.js` يحفظه/يحمّله من جدوله الصح تلقائياً بدون أي كود إضافي.

5. **حدود أمان تلقائية** (سويناها فعلاً):
   - أي صندوق تاقات: أقصى 25 عنصر (`MAX_TAGS_PER_BOX`).
   - النيكنيم: `maxlength="32"` (نفس حد ديسكورد الحقيقي).
   - أي حقل نص عادي: `maxlength` مناسب حسب المنطق (مثال: البادئة 10 أحرف).

6. **زر الحفظ**: يبدأ `class="save-btn state-default" disabled` (رمادي)،
   و`dashboard.js` يتحكم فيه تلقائياً (أحمر عند التعديل، أخضر عند النجاح
   ويرجع رمادي بعد 2.5 ثانية، أحمر "Error" عند الفشل). كل صفحة لازم تضيف
   نفس 4 كلاسات CSS (`.state-default/.state-dirty/.state-saved/.state-error`)
   بالـ `<style>` تبعها.

**خلاصة عملية:** أي صفحة HTML جديدة تلتزم بهالقواعد الست، `dashboard.js`
يشتغل عليها فوراً بدون ما نلمسه.

---

## 5. حالة `dashboard.js` الحالية (v5.0) — جاهز، هذا اللي فيه

- تحميل/حفظ تلقائي مبني على `endpointMap` (اسم صفحة HTML → جدول SQL افتراضي).
- **[جديد v5.0]** دعم أكثر من جدول بنفس الصفحة عبر `data-target-table`
  (`buildPayloadsByTable()` بدل `buildPayload()` القديمة، و`loadPageSettings()`
  تجيب كل الجداول المطلوبة بالصفحة بنداء متوازي).
- جلب رتب/قنوات السيرفر من `guild_structure_cache` (**مو** من مسارات
  `/channels` `/roles` القديمة — كانت مسارات غير موجودة أصلاً بـ
  `apiRouter.js`، اتشالت نهائياً).
- نظام تاقات موحّد (`data-id` + اسم ظاهر) يخدم 3 حالات: رتب، قنوات، نص حر.
- حد أقصى 25 تاق لكل صندوق + تنبيه بصري بسيط عند الوصول للحد.
- حالات زر الحفظ الأربعة (رمادي/أحمر/أخضر/خطأ).
- `toggleDropdown()` وإغلاق القوائم عند الضغط برّا — مركزية بالملف، أي صفحة
  تستفيد منها تلقائياً.

**القرار المتفق عليه:** ما رح نلمس `dashboard.js` أكثر لين ما نخلّص تصميم
*كل* صفحات HTML، عشان نجمع كل أنواع الحقول الغريبة (JSONB، ألوان...) ونحلها
دفعة وحدة بدل ما نرجع نعدّله كل شوي.

---

## 6. حالة `general-settings.html` — جاهزة 100%، أول صفحة اتصلحت

كل الـ `id` تتطابق مع أعمدة `setting_guild` + حقل واحد (`dashboard_access_roles`)
من `setting_management_role` عبر `data-target-table`. التصميم الأصلي محفوظ
بالكامل، بس السكربت الداخلي القديم (Mock data) اتشال وصار كل شي يعتمد على
`dashboard.js`.

---

## 7. الترتيب المتفق عليه للعمل

1. ✅ ~~general-settings.html~~ (خلصت)
2. 🔲 باقي صفحات HTML، وحدة وحدة، بالترتيب اللي يريحك. كل صفحة ترسلها لي
   للمراجعة بس (مطابقة `id`s + `data-source` + `data-target-table` لو احتاجت،
   بدون تعديل `dashboard.js`).
   - أي نوع حقل غريب (JSONB، ألوان...) → أسجله كملاحظة معلّقة، ما نحله فوراً.
   - أي حقل بدون عمود مطابق بالسكيما → أوقف وأسألك، ما أخمّن.
3. 🔲 بعد كل الصفحات: نبني نسخة نهائية من `dashboard.js` تغطي كل الحالات
   الجديدة اللي طلعت (JSONB، ألوان، إلخ).
4. 🔲 مراجعة باقي ملفات البوت والجسر (`apiRouter.js` من جديد، `client.js`،
   ملفات الأوامر، `AutoMod.js`، `AntiAlt.js`، `events/server.js`،
   `slashCommandsHandler.js`...) للتأكد كلها متوافقة مع كل شي بنيناه.
5. 🔲 **آخر شي:** بناء نظام OAuth2 (تسجيل الدخول بديسكورد) — راجع القسم 8.

---

## 8. خطة OAuth2 (مؤجّلة عمداً للنهاية، بدون تعارض مع الخطوات فوق)

**ليش التأجيل مو مشكلة:** الحماية الحالية بسيطة (`DASHBOARD_API_KEY` مشترك
يتحقق منه `apiRouter.js` بـ `requireDashboardApiKey`). كل الصفحات تشتغل
وتتفحص بفتحها يدوياً بالرابط (`general-settings.html?guildId=...`) بدون أي
تسجيل دخول. الصفحات **ما بتحتاج أي تعديل لاحقاً** لما نبني OAuth2 — هو بس
طبقة "حارس" تُضاف قبلها، مو جزء منها.

**الخطوات لما نوصلها:**
1. تسجيل تطبيق بـ Discord Developer Portal → Client ID + Secret.
2. صفحة "Login with Discord" (OAuth2 Authorization Code Flow).
3. تبادل الـ code بـ access token، جلب رتب المستخدم بالسيرفر من Discord API.
4. مقارنة رتبه مع عمود `dashboard_access_roles` (بجدول `setting_management_role`
   — **جاهز فعلاً من general-settings.html**، ينتظر بس الاستهلاك).

**⚠️ قرار معلّق لازم نحسمه وقتها:** السكيما مبنية على **Supabase Auth**
(`auth.users`, `auth.uid()`) للتحقق من الصلاحيات عبر RLS (دالة
`is_dashboard_staff()`). وقت بناء OAuth2 لازم نقرر: نربط تسجيل الدخول
بديسكورد مباشرة مع `supabase.auth.signInWithOAuth()`، أو ندير الأمر يدوياً
ونضيف صف بجدول `dashboard_staff` بأنفسنا. مرتبط بنفس سؤال نوع `SUPABASE_KEY`
(القسم 9، نقطة 2).

---

## 9. فجوات/أسئلة معلّقة (لسه ما اتحلّت)

1. **`PUT /guild/:guildId/nickname` غير موجود بـ `apiRouter.js`.** الداشبورد
   يحاول ينادي هالمسار لما يتغيّر النيكنيم، لكنه مو معرّف. النتيجة: العمود
   بقاعدة البيانات يتحدث صح، لكن ديسكورد نفسه ما يتغيّر فوراً (ينتظر حدث
   `guildMemberUpdate`/`ready` عادي). `GRS.js` عنده `enforceNickname()` جاهزة
   بالضبط لهالغرض — بس محتاجة route يستدعيها.

2. **نوع `SUPABASE_KEY` غير مؤكد** (anon أو service_role؟). لو anon، كل
   الـ RLS Policies اللي تعتمد على `auth.uid()` بترفض كل قراءة/كتابة بصمت
   لأن العمليات هذي مو جاية من مستخدم مسجل دخول بـ Supabase Auth، جايه من
   الـ API Key بس. لازم تأكيد قبل ما نكمل أي اختبار حقيقي.

3. **ملفات الأوامر** (`ban.js`, `mute.js`, `warn.js`, `kick.js`, `delete.js`)
   لسه ما وصلت — `commandhandler.js` يتوقع منها `run(message, dbUtils)` +
   `aliases` array اختياري (لـ `unban`/`unmute`/`unwarn`/`clear`)، بس ما
   نقدر نأكد إنها متوافقة فعلاً بدون نشوفها.

4. **دالة `dbUtils.getGuildSettings()`** يستخدمها `GRS.js` — موجودة فعلاً
   بـ `dbUtils.js` (`v3.2`) وترجع `queries.universalGet('setting_guild', guildId)`،
   فهذي محلولة ✅ (كانت فجوة مشكوك فيها بمراجعة سابقة، تأكدنا إنها موجودة).

---

## 10. آخر شي طلبته صاحب المشروع

> "اخر شيء بسلم ملفات apiRouter.js ... etc"

يعني: **`apiRouter.js` وملفات الجسر/البوت الباقية تُسلَّم كمرحلة أخيرة**،
بعد ما تخلص كل صفحات الداشبورد وتتأكد الشكل والحفظ شغالين صح. هذا متوافق
100% مع الترتيب بالقسم 7 فوق.






شوف الخطة ولا تصنع اي كود الان
تمام، هذا ملخص جاهز تنسخه بالكامل وتفتح فيه محادثة جديدة:
📌 ملاحظة مهمة (لازم تنتبه لها بأي كود جديد)
المحادثة بيننا بالعربي، لكن أي رسالة تظهر للمستخدم النهائي داخل بوت الديسكورد (embeds, رسائل الترحيب/المغادرة، رسائل الأوامر، أي محتوى يشوفه العضو بالسيرفر) لازم تكون بالإنجليزي. التعليقات بالكود (comments) ممكن تفضل عربي للتوضيح.
📌 المفهومان الأساسيان اللي أشتغل عليهم دايماً

* Smart Binding: `id` بالـ HTML = اسم عمود SQL = مفتاح JSON، عشان `dashboard.js` يجمع الفورم ويرسله كـ JSON والـ API الموحّد `/api/guild/:guildId/:tableName` يحدّث بدون كود خاص لكل حقل.
* Auto Loading: `commandhandler.js` يحمّل تلقائياً أي ملف بمجلد `commands/AutoMod/`، والملفات اللي مو أوامر شات حقيقية (`AutoMod.js`, `AntiAlt.js`, `GRS.js`) مُستثناة بالاسم من التسجيل كأوامر (هذا الاستثناء مُطبَّق فعلياً حالياً).

تنويه ممكن يكون في خطا ترا مافي اي مجلد باسم AutoMod مافي اي مجلد باسم AutoMod مافي مافي مافي هذا كان قديم وتم حذفه كان قديم وتم حذفه الملفات لم تحدث لذلك

شوف الخطة ولا تصنع اي كود الان
تمام، هذا ملخص جاهز تنسخه بالكامل وتفتح فيه محادثة جديدة:
📌 ملاحظة مهمة (لازم تنتبه لها بأي كود جديد)
المحادثة بيننا بالعربي، لكن أي رسالة تظهر للمستخدم النهائي داخل بوت الديسكورد (embeds, رسائل الترحيب/المغادرة، رسائل الأوامر، أي محتوى يشوفه العضو بالسيرفر) لازم تكون بالإنجليزي. التعليقات بالكود (comments) ممكن تفضل عربي للتوضيح.
📌 المفهومان الأساسيان اللي أشتغل عليهم دايماً

* Smart Binding: `id` بالـ HTML = اسم عمود SQL = مفتاح JSON، عشان `dashboard.js` يجمع الفورم ويرسله كـ JSON والـ API الموحّد `/api/guild/:guildId/:tableName` يحدّث بدون كود خاص لكل حقل.
* Auto Loading: `commandhandler.js` يحمّل تلقائياً أي ملف بمجلد `commands/AutoMod/`، والملفات اللي مو أوامر شات حقيقية (`AutoMod.js`, `AntiAlt.js`, `GRS.js`) مُستثناة بالاسم من التسجيل كأوامر (هذا الاستثناء مُطبَّق فعلياً حالياً).

تنويه ممكن يكون في خطا ترا مافي اي مجلد باسم AutoMod مافي اي مجلد باسم AutoMod مافي مافي مافي هذا كان قديم وتم حذفه كان قديم وتم حذفه الملفات لم تحدث لذلك








